# =========================================================
# 斐讯 K2 核心硬件与芯片架构配置 (MT7620A)
# =========================================================
CONFIG_TARGET_ramips=y
CONFIG_TARGET_ramips_mt7620=y
CONFIG_TARGET_ramips_mt7620_DEVICE_phicomm_k2-v22.4=y

# =========================================================
# 极致精简 LuCI：仅保留最原始的网页框架与中文语言
# =========================================================
CONFIG_MODULES=y
CONFIG_PACKAGE_luci=y
CONFIG_PACKAGE_luci-light=y
CONFIG_PACKAGE_luci-mod-admin-full=y
CONFIG_LUCI_LANG_zh_Hans=y
CONFIG_PACKAGE_luci-theme-bootstrap=y

# =========================================================
# 无线中继与安全组件：完美支持正常 WiFi 密码和无线桥接
# =========================================================
CONFIG_PACKAGE_wpad-basic-mbedtls=y

# =========================================================
# 恢复核心拨号组件：完美支持传统 PPPoE 宽带拨号
# =========================================================
CONFIG_PACKAGE_ppp=y
CONFIG_PACKAGE_ppp-mod-pppoe=y

# =========================================================
# 必备网络组件与 完整 IPv6 支持
# =========================================================
CONFIG_IPV6=y
CONFIG_PACKAGE_ipv6helper=y
CONFIG_PACKAGE_dnsmasq_full_dhcpv6=y
CONFIG_PACKAGE_odhcp6c=y
CONFIG_PACKAGE_odhcpd-ipv6only=y
CONFIG_PACKAGE_ip6tables=y

# =========================================================
# 极度瘦身：不在这里强行编译体积巨大的 Lucky 二进制包
# =========================================================
# CONFIG_PACKAGE_lucky is not set
# CONFIG_PACKAGE_luci-app-lucky is not set
